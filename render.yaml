services:
  - type: web
    name: ssserver-relay
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn relay:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
